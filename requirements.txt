# SNAPBACK / ReMotion — Python dependencies
# Install from repo root:  pip install -r requirements.txt

# ── API server ────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
python-multipart>=0.0.9

# ── AI (exercise plan generation) ────────────────────────────
anthropic>=0.25.0

# ── Pose estimation (stub — uncomment when MediaPipe is wired)
# mediapipe>=0.10.0
# opencv-python>=4.8.0
# numpy>=1.24.0
